/// Set up Global Variables

score = 0;
lives = 3;
global.level = 1;
global.levels_completed = 0;
global.gameover = 0;
global.gamepad = 0; // game pad number, have to change to detect later
audio_master_gain(0.6); // set global volume

// Load first level IF YOU CLICK START

// room_goto_next();

